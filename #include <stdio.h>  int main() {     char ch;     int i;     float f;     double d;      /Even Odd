#include <stdio.h>

int main() {
    int N;

    // Input the number
    scanf("%d", &N);

    // Check even or odd
    if (N % 2 == 0) {
        printf("Even\n");
    } else {
        printf("Odd\n");
    }

    return 0;
}
