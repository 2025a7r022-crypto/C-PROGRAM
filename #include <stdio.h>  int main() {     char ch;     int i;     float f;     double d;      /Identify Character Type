#include <stdio.h>
#include <ctype.h>

int main() {
    char ch;

    // Input a character
    scanf("%c", &ch);

    // Check category
    if (isalpha(ch)) {
        printf("Alphabet\n");
    } else if (isdigit(ch)) {
        printf("Digit\n");
    } else {
        printf("Symbol\n");
    }

    return 0;
}
