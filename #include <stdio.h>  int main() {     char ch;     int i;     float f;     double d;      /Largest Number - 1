#include <stdio.h>

int main() {
    int A, B;

    // Input two numbers
    scanf("%d %d", &A, &B);

    // Compare and print the larger number
    if (A >= B) {
        printf("%d\n", A);
    } else {
        printf("%d\n", B);
    }

    return 0;
}
